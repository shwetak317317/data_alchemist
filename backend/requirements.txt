# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# LLM (LiteLLM for multi-provider support)
litellm==1.48.7
anthropic>=0.35.0

# Agent orchestration
langgraph==0.2.28
langchain-core==0.3.10

# Database — metadata store
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3

# Configuration
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
pyyaml==6.0.2
jinja2==3.1.4                     # prompt template rendering

# Encryption for stored credentials
cryptography==43.0.1

# Scheduling
apscheduler==3.10.4

# Data connectors
pyodbc==5.1.0                     # SQL Server
snowflake-connector-python==3.12.1
databricks-sql-connector==3.4.0
psycopg2-binary==2.9.9            # PostgreSQL (covered above)
duckdb==1.1.1
# google-cloud-bigquery==3.25.0   # uncomment when BigQuery connector is built

# HTTP
httpx==0.27.2

# Utilities
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# SQL parsing — query-log lineage discovery (extract source/target tables from
# INSERT/MERGE/CREATE TABLE AS SELECT statements, never regex-guessed)
sqlglot==25.24.5
